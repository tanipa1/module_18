var numbers = [45, 30, 12, 55, 80];

// get element by index
console.log(numbers[1]);

var element = numbers[3];
console.log(element);

// array element replacing
numbers[2] = 77;
numbers[4] = 1;

console.log(numbers);

// find index of an element
var positionIndex = numbers.indexOf(77);
console.log(positionIndex);
